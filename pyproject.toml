[build-system]
requires = ["setuptools>=62.0"]
build-backend = "setuptools.build_meta"

[project]
name = "engine"
version = "0.1.0"
description = "The ToolCraft processing engine."
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["engine*"]